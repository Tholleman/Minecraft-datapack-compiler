package compiler.cleaner;

import compiler.FileStrings;

public class CleanerStrings extends FileStrings
{
	public static final String MISSING_SOURCE_FILES = "Can not in good conscience clean files when " + SOURCE_DIRECTORY + " or " + CONFIG_PATH + " are missing";
}
